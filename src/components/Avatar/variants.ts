import { css } from 'styled-components';

export const SIZES = {
  sm: css`
    --avatar-width: 16px;
    --avatar-height: 16px;
  `,
  md: css`
    --avatar-width: 32px;
    --avatar-height: 32px;
  `,
  lg: css`
    --avatar-width: 120px;
    --avatar-height: 120px;
  `,
};
